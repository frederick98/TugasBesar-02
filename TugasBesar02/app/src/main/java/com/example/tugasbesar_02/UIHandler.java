package com.example.tugasbesar_02;


import android.os.Handler;
import android.os.Message;

/*
public class UIHandler extends Handler {

    protected MainActivity activity;
    protected final static int MSG_SET_GAMBAR=0;


    public UIHandler(MainActivity activity)
    {
        this.activity=activity;
    }

    public void setMusuhMove(Musuh musuh)
    {
        Message msg = new Message();
        msg.what = MSG_SET_GAMBAR;
        msg.obj = musuh;
        this.sendMessage(msg);
    }

    @Override
    public void handleMessage(Message msg) {
        if(msg.what==UIHandler.MSG_SET_GAMBAR )
        {
            Musuh musuh = (Musuh)msg.obj;
            //this.activity.drawMusuh(musuh);
        }
    }
}

 */
